#This is a test JavaScript file to check the vulnerability scanner

console.log(Hello, this is a test script.);

Possible security vulnerabilities for testing
var userInput = test;
eval(userInput);  Potential security risk Use of eval

const fs = require(fs);
fs.readFile(etcpasswd, utf8, (err, data) = {  Potential security risk Unauthorized file access
    if (err) {
        console.error(err);
        return;
    }
    console.log(data);
});

 End of test script